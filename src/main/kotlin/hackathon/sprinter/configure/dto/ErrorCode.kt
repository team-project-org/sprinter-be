package hackathon.sprinter.configure.dto

enum class ErrorCode {
    UNKNOWN_ERROR,
    INVALID_PARAMETER,
    ITEM_NOT_EXIST,
    MEMBER_NOT_EXIST,
    WRONG_PASSWORD,
    PARAMETER_INVALID,
    JWT_CREATE_FAIL,
    AUTHENTICATE_FAIL,
    ACCESS_DENIED,
}
